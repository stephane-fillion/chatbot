# cat=basic; type=options[OpenAI=openai,MistralAI=mistralai]; label=IA
endpoint = openai

# cat=basic; type=string; label=Api key
apikey = 

# cat=basic; type=string; label=Model
model = gpt-3.5-turbo

# cat=basic; type=boolean; label=Activate in BE
be_activation = 1

ratelimiter {

    # cat=ratelimiter; type=boolean; label=Activate rate limiter: See https://symfony.com/doc/current/rate_limiter.html
    activation = 1

    # cat=ratelimiter; type=string; label=Policy
    policy = sliding_window

    # cat=ratelimiter; type=integer; label=LImit
    limit = 30

    # cat=ratelimiter; type=string; label=INterval for fixed_window and sliding_window
    interval = 15 minutes

    # cat=ratelimiter; type=string; label=For token_bucket policy. Must be an JSON configuration
    rate = { interval: '15 minutes', amount: 500 }
}